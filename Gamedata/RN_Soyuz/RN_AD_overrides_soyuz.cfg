@PART[rn_va_dec]:NEEDS[AnimatedDecouplers] {
	!MODULE[ModuleDecouple]
    {
	}
	!MODULE[FSanimateGeneric]
    {
	}
	MODULE
	{
	    name = ModuleAnimatedDecoupler
	    ejectionForce = 50
	    explosiveNodeID = top
	    staged = true
	    animationName = Deploy
		waitForAnimation = false // (If true, then delay decoupling until animation has finished playing)
		layer = 1 // Animation's layer will be set to this. Helps prevent multiple animations on one part interfering with each other
	}
}

@PART[rn_vostok_dec]:NEEDS[AnimatedDecouplers] {
	!MODULE[ModuleDecouple]
    {
	}
	!MODULE[FSanimateGeneric]
    {
	}
	MODULE
	{
	    name = ModuleAnimatedDecoupler
	    ejectionForce = 40
	    explosiveNodeID = top
	    staged = true
	    animationName = Deploy
		waitForAnimation = false // (If true, then delay decoupling until animation has finished playing)
		layer = 1 // Animation's layer will be set to this. Helps prevent multiple animations on one part interfering with each other
	}
}
